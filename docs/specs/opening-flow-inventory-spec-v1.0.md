# Opening Flow Inventory Spec v1.0 - Deprecated

Date: 2026-07-06

This old inventory prompt discussed a possible `StampPair` transition after
opening the night letter. It is not a current implementation instruction.

Current canon:

- `/home` opened state uses the normal `desk-home-frame`.
- `home-stamp-pair` and `home-stamp-pair-stamp` must stay absent.
- Opened deliveries do not return to the notification tray.
- Re-access is through tapping the normal home frame.

Use `docs/specs/opening-flow-v2.md` for the current rule.
